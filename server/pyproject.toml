[project]
name = "air-data-server"
version = "0.1.0"
description = ""
authors = [
    {name = "Eemil Sinkko",email = "eemil.sinkko@helsinki.fi"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]

[tool.poetry]
packages = [{include = "air_data_server", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
